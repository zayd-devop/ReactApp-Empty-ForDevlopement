# I - Partie théorique:  #
1. Quelle est la principale raison d'utiliser les Hooks en React?
    A. Remplacer Redux
    B. Ajouter des fonctionnalités aux classes
    C. Utiliser l'état et d'autres fonctionnalités sans classe
    D. Accélérer le rendu des composants

2. Quelle est la différence principale entre useState et useReducer ?
    A. useState est synchrone alors que useReducer est asynchrone
    B. useReducer est plus adapté aux états complexes
    C. useState est utilisé dans les classes alors que useReducer est pour les fonctions
D. Il n'y a aucune différence
3. À quoi sert le Hook useEffect?
    A. Remplacer les événements du DOM
    B. Effectuer des effets de bord dans un composant fonctionnel
    C. Gérer l'état local
    D. Créer des composants React
4. Comment exécuter useEffect uniquement lors du montage du composant?
    A. useEffect(callback)
    B. useEffect(callback, [])
    C. useEffect(callback. Istatel)